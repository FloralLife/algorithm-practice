#include <iostream>
using namespace std;

int N, C, X[200000];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> N >> C;
	for (int i = 0; i < N; i++) {
		cin >> X[i];
	}


	return 0;
}